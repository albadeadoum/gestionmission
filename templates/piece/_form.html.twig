{#
               {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <button class="btn btn-success" style="margin-right: 10px;">{{ button_label|default('Enregistrer') }}</button>
                    <button type="reset" class="btn btn-secondary" style="float: right;">{{ button_label2|default('Vider') }}</button>
                {{ form_end(form) }}
#}
{{ form_start(form, {
    'attr': {'id': 'editPieceForm'},
    'action': path('app_piece_edit', {'id': piece.id}),
    'method': 'POST'
}) }}
    {{ form_widget(form._token) }}

    
   
    <button type="submit" class="btn btn-success mt-3" > <i class="fas fa-save"></i> Mettre à jour </button>
{{ form_end(form) }}
