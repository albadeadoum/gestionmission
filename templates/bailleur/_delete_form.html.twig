<form method="post" action="{{ path('app_bailleur_delete', {'id': bailleur.id}) }}" onsubmit="return confirm('êtes vous sûr(e) de vouloire suuprimer cet Agent de la base?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bailleur.id) }}">
    <button class="btn btn-danger"><i class="fa-solid fa-trash" ></i></button>
</form>
