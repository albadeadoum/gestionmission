{% extends 'base.html.twig' %}

{% block title %}Bailleur{% endblock %}

{% block body %}
    <h1>Bailleur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bailleur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ bailleur.nom }}</td>
            </tr>
            <tr>
                <th>Taux_ag</th>
                <td>{{ bailleur.tauxAg }}</td>
            </tr>
            <tr>
                <th>Taux_ox</th>
                <td>{{ bailleur.tauxOx }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bailleur_index') }}">back to list</a>

    <a href="{{ path('app_bailleur_edit', {'id': bailleur.id}) }}">edit</a>

    {{ include('bailleur/_delete_form.html.twig') }}
{% endblock %}
