<form method="post" action="{{ path('app_chauffeur_delete', {'id': chauffeur.id}) }}" onsubmit="return confirm('êtes vous sûr(e) de vouloire suuprimer cette Chauffeur de la base?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chauffeur.id) }}">
    <button class="btn btn-danger"><i class="fa-solid fa-trash" ></i></button>
   
</form>
