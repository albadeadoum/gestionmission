<form method="post" action="{{ path('app_kilometre_delete', {'id': kilometre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kilometre.id) }}">
    <button class="btn">Delete</button>
</form>
