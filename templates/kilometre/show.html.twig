{% extends 'base.html.twig' %}

{% block title %}Kilometre{% endblock %}

{% block body %}
    <h1>Kilometre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kilometre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ kilometre.Nom }}</td>
            </tr>
            <tr>
                <th>Kilometre</th>
                <td>{{ kilometre.kilometre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_kilometre_index') }}">back to list</a>

    <a href="{{ path('app_kilometre_edit', {'id': kilometre.id}) }}">edit</a>

    {{ include('kilometre/_delete_form.html.twig') }}
{% endblock %}
