{% extends 'base.html.twig' %}

{% block title %}UserAction{% endblock %}

{% block body %}
    <h1>UserAction</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_action.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ user_action.description }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ user_action.createdAt ? user_action.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_action_index') }}">back to list</a>

    <a href="{{ path('app_user_action_edit', {'id': user_action.id}) }}">edit</a>

    {{ include('user_action/_delete_form.html.twig') }}
{% endblock %}
