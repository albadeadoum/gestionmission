
<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <style>
    @page {
        size: A4 landscape;
        margin: 10mm;
    }

    body {
      font-family: 'Calibri', serif;
      font-size: 9pt;
      line-height: 1.2;
      color: #000;
      margin: 0;
      padding: 0;;
    }

    .title {
        text-align: center;
        font-weight: bold;
        margin: 20px 0;
        text-decoration: underline;
        font-size: 16pt;
    }

    .header {
      text-align: center;
      margin-bottom: 10px;
    }

    table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
        font-size: 9pt;   
    }

    th, td {
        border: 1px solid #000;
        padding: 6px;
        text-align: left;
        vertical-align: top;
        font-size: 11pt;
    }

    th {
        background-color: #f0f0f0;
    }

    .label {
        width: 25%;
        font-weight: bold;
        border: none;
    }

    .no-border td {
        border: none;
        padding: 3px;
    }

    .signature {
        margin-top: 40px;
        text-align: right;
    }

    h2 {
        margin-bottom: 10px;
        font-size: 14pt;
    }

    th.nom, td.nom { width: 12%; }
    th.prenom, td.prenom { width: 12%; }
    th.service, td.service { width: 20%; }
    th.jours, td.jours { width: 8%; }
    th.montant, td.montant { width: 16%; }
    th.paiement1, td.paiement1 { width: 16%; }
    th.paiement2, td.paiement2 { width: 16%; }

    .table-title {
      text-align: center;
      font-weight: bold;
      text-decoration: underline;
      margin: 10px 0 10px 0;
    }
  </style>


</head>
<body>
  <div class="header">
            <table>
                <tr>
                    <td style="width: 13%; vertical-align: middle;">
                        <div class="header-logo"><img src="{{ absolute_url(asset('ins.jpg')) }}" alt="Logo INS Niger" style="width: 120px; height: auto; margin: 5px 0;"></div>
                    </td>
                    <td style="width: 87%; vertical-align: middle;" class="header-text">
                        <p style="font-size: 12pt; margin: 3px 0;">RÉPUBLIQUE DU NIGER</p>
                        <p style="font-size: 12pt; font-weight: bold; margin: 3px 0;">Fraternité - Travail - Progrès</p>
                        <p style="font-size: 12pt; font-weight: bold; margin: 3px 0;">MINISTÈRE DE L'ECONOMIE ET DES FINANCES</p>
                        <p style="font-size: 12pt; font-weight: bold; margin: 3px 0;">INSTITUT NATIONAL DE LA STATISTIQUE</p>
                        <p style="font-size: 11pt; margin: 3px 0;">Établissement Public à Caractère Administratif</p>
                        <p style="font-size: 12pt; font-weight: bold; margin: 3px 0;">BUREAU CENTRAL DE RECENSEMENT</p>
                        <p style="font-size: 11pt; margin: 3px 0;">Unité Des Ressources Humaines</p>
                    </td>
                </tr>
            </table>
            <div class="bottom-image">
              <img src="{{ absolute_url(asset('im2.png')) }}" alt="Logo secondaire INS Niger">
            </div>
            <hr class="divider">
        </div>
  <h2 class="title">{{ evenement.titre }}</h2>

  

  <h2>État de mission - {{ evenement.titre }}</h2>
  <p>Période : {{ evenement.debut|date('d/m/Y') }} au {{ evenement.fin|date('d/m/Y') }}
   ({{ nbJours }} jours)</p>

  <table>
      <thead>
          <tr>
              <th>N°</th>
              <th>Nom et Prénom</th>
              <th>Fonction</th>
              <th>Nb de jours</th>
              <th>TAUX/jours</th>
              <th>Montant total</th>
              <th>Avance (90%)</th>
              <th>Reliquat (10%)</th>
              <th>N° CIN</th>
              <th>Emargement</th>
          </tr>
      </thead>
      <tbody>
          {% for p in participants %}
              <tr>
                  <td></td>  
                  <td>{{ p.nom }} {{ p.prenom }}</td>
                  <td>{{ p.fonction }}</td>
                  <td>{{ p.jours }}</td>
                  <td></td>
                  <td>{{ p.montant|number_format(0, ',', ' ') }} F CFA</td>
                  <td>{{ p.paiement1|number_format(0, ',', ' ') }} F CFA</td>
                  <td>{{ p.paiement2|number_format(0, ',', ' ') }} F CFA</td>
                  <td></td>
                  <td></td>
              </tr>
          {% endfor %}
      </tbody>
  </table>

  <div class="signature">
      Fait à Niamey, le {{ date_today }}<br><br>
      <strong>Le Directeur Général p.i</strong><br><br><br>
      <strong>Souleymane ALZOUMA</strong>
  </div>

</body>
</html>




<!-- 
<!doctype html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <style>
    @page {
        size: A4 landscape;
        margin: 15mm;
    }
     body {
      font-family: 'Calibri', serif;
      font-size: 12pt;
      line-height: 1.4;
      color: #000;
      margin: 0;
      padding: 0;
    }
    .title { text-align:center; font-weight: bold; margin: 20px 0; text-decoration: underline; }
    table { width: 100%; border-collapse: collapse; margin-bottom: 15px; }
    td, th { padding: 6px; vertical-align: top; }
    .label { width: 35%; font-weight: bold; }
    .signature { margin-top: 40px; text-align: right; }
    .bordered th, .bordered td { border: 1px solid black; }
  </style>
</head>
<body>
  <h2 class="title">ÉTAT DE PAIEMENT </h2>

  <table>
    <tr><td class="label">Objet de la mission</td><td>{{ evenement.titre }}</td></tr>
    <tr><td class="label">Date début</td><td>{{ evenement.debut|date('d/m/Y') }}</td></tr>
    <tr><td class="label">Date fin</td><td>{{ evenement.fin|date('d/m/Y') }}</td></tr>
    <tr><td class="label">Destination</td><td>{{ evenement.destination }}</td></tr>
    <tr><td class="label">Bailleur</td><td>{{ evenement.bailleur.nom }}</td></tr>
  </table>

  <h2>État de mission - {{ evenement.titre }}</h2>
<p>Période : {{ evenement.debut|date('d/m/Y') }} au {{ evenement.fin|date('d/m/Y') }}
   ({{ nbJours }} jours)</p>

<table border="1" cellspacing="0" cellpadding="5">
    <thead>
        <tr>
            <th>Nom</th>
            <th>Prénom</th>
            <th>Service / Fonction</th>
            <th>Nb jours</th>
            <th>Montant total</th>
            <th>Paiement 1 (90%)</th>
            <th>Paiement 2 (10%)</th>
        </tr>
    </thead>
    <tbody>
        {% for p in participants %}
            <tr>
                <td>{{ p.nom }}</td>
                <td>{{ p.prenom }}</td>
                <td>{{ p.service }}</td>
                <td>{{ p.jours }}</td>
                <td>{{ p.montant|number_format(0, ',', ' ') }} F CFA</td>
                <td>{{ p.paiement1|number_format(0, ',', ' ') }} F CFA</td>
                <td>{{ p.paiement2|number_format(0, ',', ' ') }} F CFA</td>
            </tr>
        {% endfor %}
    </tbody>
</table>



  <div class="signature">
    Fait à Niamey, le {{ date_today }}<br><br>
    <strong>Le Directeur Général p.i</strong><br><br><br>
    <strong>Souleymane ALZOUMA</strong>
  </div>
</body>
</html>
-->